using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class State : MonoBehaviour {

    public bool use = false; // 기본값을 false로 두어 아이템을 사용하지 않은 상태로 둔다.
    public int itemnum; //어떤 아이템을 사용했을 경우 상태를 바꾸는 가에 대해 필요한 아이템 넘버

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
